# GCP builds submit — keep uploads fast by excluding heavy/unused files
# Cloud Build runs npm install / pip install on its own fast servers — no need to upload these

# Dependencies (Cloud Build installs these)
**/node_modules/
**/apps/api/.venv/
**/.venv/

# Mobile app (heavy binaries, not needed for Web or API builds)
**/apps/mobile/android/
**/apps/mobile/ios/

# Build outputs (regenerated by Cloud Build)
**/apps/web/.next/
**/.expo/
**/.turbo/
**/dist/
**/build/

# Git
.git/

# Env (secrets — use Secret Manager or Cloud Build substitutions)
.env*
!.env.example

# Misc
*.md
*.log
docs
.gitignore
